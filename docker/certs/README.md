Letakkan sertifikat CA tambahan (`*.crt`, format PEM) di folder ini **hanya** bila build Docker berjalan di
jaringan dengan proxy TLS (mis. jaringan kantor). File `*.crt` tidak ikut ke git. Kosongkan untuk build normal.
